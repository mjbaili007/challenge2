/*Closure Intro: Write a function createMultiplier(multiplier) that returns a new arrow function.
 The returned function should take a number and multiply it by the original multiplie */

const r = require ("prompt-sync")()
let x  = r("donner moi une nbr");
let y = r("donner moi  la 2 nbr");
function calculate(a, b, operation) {
    return operation
}