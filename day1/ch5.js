/*Max of Three: Declare three number variables. 
Write conditions to find and log the largest of the three numbers without using Math.max */
let a = 34 ;
let b = 3;
let c = 0 ; 
let max = c ; 
if (a > c ){
    console.log(" max est " + a );
} else if (b > c ){
    console.log(" max est " + b );
} else {
    console.log(" max est " + b );
}
