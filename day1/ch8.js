/*Valid Triangle: Declare three variables representing the angles of a triangle.
 Check if they form a valid triangle
 (all three add up to exactly 180, and no angle is 0 or less). */
let an1 = 50 ; 
let an2 = 70 ; 
let an3 = 70 ; 
if (an1 + an2 +an3 === 180 ){
    console.log(" c'est un tringle ");
}else 
    console.log(" c'est pas un tringle ");